# Plan B Pre-Promotion Scan Summary — 2026-04-13

Trivy v0.69.3, severity HIGH+CRITICAL only. Java DB + standard DB included.

| Image | CRITICAL | HIGH | Components | Action |
|---|---:|---:|---:|---|
| cyberchef-10.22.1 | 0 | 9 | 1 | tolerable — upgrade later |
| jellyfin-2026-04-12 | 0 | 10 | 2 | tolerable — upgrade later |
| mysql-8.0 | 1 | 7 | 3 | upgrade upstream pin before promote |
| nomad-2026-04-12 | 32 | 336 | 3 | REJECT — replace/upgrade required |
| qdrant-1.16 | 5 | 42 | 3 | upgrade upstream pin before promote |
| redis-7-alpine | 4 | 39 | 2 | upgrade upstream pin before promote |
| s7-fedora-base-v2026.04.13 | 0 | 0 | 0 | CLEAN — promote |

**Totals: 42 CRITICAL / 443 HIGH**

## What's clean

- `s7-fedora-base:v2026.04.13` — the S7-built bootc image. The work S7 controls is at zero.

## What's dirty

Every third-party upstream image. The ones we currently run on Tonya's QUBi.
The intake-gate strategy is validated by this finding: putting these under
the S7 signature does NOT fix the underlying CVEs. We need to upgrade upstream
pins (or replace the components) BEFORE sealing them with the S7 brand.

## What is NOT scanned tonight

- The pod-internal containers (admin SPA, postgres, ollama models)
- Vivaldi (still runs as host binary, no container yet)
- The bootc base layer history (only the final tag was scanned)

## Path C attempt — fresh nomad re-pull

**Result: same digest, no fix available upstream.**

```
upstream: ghcr.io/crosstalk-solutions/project-nomad:latest
auth:     ghcr.io login as skycair-code (worked)
digest:   sha256:c4c6a38b875f4d1ecab5cd5e05d04c3ed89a4fefdc7772864f28464bf796edb1
created:  2026-04-03 21:31:34 UTC
```

The fresh pull returned the exact same config digest as the version we already
have running and the version the original Plan B.2 scan flagged with 32
CRITICAL + 336 HIGH CVEs. Upstream has not shipped a new release since
2026-04-03. **Re-pulling cannot fix what upstream has not fixed.**

Real options for nomad, none of which fit in tonight's budget:

1. **Fork project-nomad and patch the bundled deps.** Big scope. Touches a
   third-party Java/Node codebase we don't own.
2. **Replace the backend.** Swap project-nomad for upstream n8n directly, or
   for automatisch, or for another orchestrator. Big scope. Affects every
   workflow currently running against the SPA.
3. **Risk-accept and document.** Note that nomad runs inside the qubi network
   (so blast radius is limited to the qubi network) and runs without internet
   egress at runtime. A risk acceptance here is defensible IF the network
   isolation holds.
4. **Drop nomad entirely.** If S7 doesn't actually need a workflow engine in
   the shipping QUBi, removing it is the cleanest fix. Cube-side decision.

This is image-hardening work, not Plan B work. Pin a separate plan.
