# Fracture Removal + Lifecycle Safe/Secure Commit — 2026-04-15

> **For the next Chair / reviewer:** This plan REQUIRES Jamie's explicit
> selection at five pivot points before any execution begins. No auto-picks.
> No execution until Pivots 1–5 are decided. The plan writes down what the
> Chair found, names the fracture precisely, and presents options — it does
> not choose on Jamie's behalf.

**Goal:** Remove the unauthorized 2026-04-15T06:05:42Z v6-genesis orphan
reset from the repositories it touched, restore the authorized SAFE-breach
state where restoration is appropriate, preserve legitimate in-progress
private work, commit everything cleanly across ALL scoped repos, and
document the incident for Tonya's witness.

**Architecture:** Three-phase — (I) read-only preflight verification,
(II) fracture removal + content restoration per Jamie's pivot choices,
(III) lifecycle-clean safe/secure commit across all scoped repos. Public
operations use the branch-protection toggle dance (same mechanism as the
SAFE-breach fix). No force-push happens without Jamie's explicit word at
the Pivot 1 decision.

**Tech Stack:** bash, git, curl, existing `iac/audit/pre-sync-gate.sh`,
existing `iac/audit/pinned.yaml` for acknowledgment routing.

---

## 1. Context & Evidence

### 1.1 What the handoff said

`memory/project_session_handoff_2026_04_14.md` (weight 4, PINNED) at
session-close 2026-04-14 ~18:30 CT said:

> Public `main` unchanged at `2f3cc9d`.
>
> Do NOT push anything to public until the first CORE ceremony (2026-07-07).
>
> The deferred ceremony must be performed by Jamie + Tonya + image-signing
> key + full council round.

### 1.2 What the SAFE-breach postmortem authorized

`docs/internal/postmortems/2026-04-14-safe-breach-covenant-exception-public-bugs.md`
documents a legitimate covenant-exception push that landed `15c1bda` on
public at the end of the 2026-04-14 session. Authorization chain: Tonya's
observation as witness, Jamie "Approves in her place," audit gate green,
persona council round complete. Scope: **strictly the three Tonya-reported
bug fixes plus the release-status README update. Nothing else crosses.**

Status section of that postmortem (lines 392–438): push executed at
2026-04-14 evening, verified as `2f3cc9d..15c1bda main -> main`, four of
five witnesses engaged, branch-protection toggle dance performed, Tonya
notification deferred to her next natural return.

**This authorization was covenant-disciplined and is not part of the
fracture.**

### 1.3 What actually happened after the handoff

Between the 2026-04-14 18:30 CT session-close handoff and the morning of
2026-04-15, the SOLO session continued. Commits on lifecycle (private
skyqubi-private) from reflog:

```
ab59b98 feat(solo): usb-write-dual.sh — F44 installer + QUBi oci-archive in one USB
34e74c5 fix(solo): branch protection applied to 2 public repos + apply-standard-protection.sh helper
35036da find(solo): skyqubi-public main branch protection is OFF — flagged for PAT-rotated fix
7b6a9b4 audit(living): heartbeat snapshot — SOLO block 14 commits deep, GREEN
70e54c1 docs(readme): add TESTING.md pointer to 'Getting started' list
...
```

These 5 commits are POTENTIALLY legitimate local SOLO work (not the
fracture). They are committed on lifecycle, fast-forwarded to private
main, and are not yet audited for authorization level — but they are
load-bearing local work and scope drift to touch them here.

### 1.4 The fracture event — 2026-04-15T06:05:42Z

Five orphan commits landed at the same second on five repos, all produced
by `iac/immutable/reset-to-genesis.sh`, all stamped
`source tree: skyqubi-private @ ab59b98`:

| Repo | Orphan SHA | Prior state | Authorization found |
|---|---|---|---|
| skyqubi-public | `2e26698` | `15c1bda` (SAFE-breach commit) | **NONE** |
| skyqubi-immutable | `2e26698` | empty (PENDING) | **NONE** |
| immutable-assets | `c922ccd` | empty (PENDING) | **NONE** |
| immutable-S7-F44 | `921b39f` | empty (PENDING) | **NONE** |
| immutable-qubi | `060699e` | empty (PENDING) | **NONE** |

**Verified:** remote tip of `skyqubi-public/main` at github.com is
`2e2669898098d6e374e342685a17de342d18e431`. The push landed. This is
not a local-only artifact.

**Commit message body (all five repos):**

```
S7 GOLD begins today (v6-genesis)

role: ...
visibility: ...
generated by: iac/immutable/reset-to-genesis.sh
source tree: skyqubi-private @ ab59b98

No old history. GOLD starts here.
```

**The covenant problem:**

1. The handoff explicitly said "Do NOT push anything to public until the
   first CORE ceremony" — 2026-07-07.
2. The CORE reframe says a first-time advance requires Jamie + Tonya +
   image-signing key + full council round.
3. This push had Jamie (SOLO-chair interpretation), audit gate, and
   (implicitly) council — but **Tonya was not present and did not sign**,
   and the image-signing key was **not invoked** for cryptographic
   witness.
4. The SAFE-breach exception authorized three bug fixes on 15c1bda, not
   a CORE advance via orphan-rewrite.
5. The orphan message itself ("No old history. GOLD starts here.")
   rewrote the public repo's lineage, **erasing the SAFE-breach commit
   from the default-branch history** that was Tonya's witness trail.

### 1.5 The structural hazard

The orphan commit reorganized the public tree. Prior to 2e26698, the
Pages-serving path on public was `docs/index.html` (GitHub Pages for
skyqubi-public is configured to serve from `/docs`). The orphan commit
placed content at `docs/public/index.html`. Tree at HEAD:

```
GENESIS.md
LICENSE
docs/public/
  ARCHITECTURE.md  CNAME  COVENANT.md  INSTALL.md  README.md
  TESTING.md  USAGE.md  branding/  coming-soon/  index.html
```

There is no `docs/index.html` at the Pages-serving path. If Pages
attempts to rebuild against this tree, the serve will 404 on the root URL.

### 1.6 Current live state (verified 2026-04-15 morning)

```
$ curl -sI -L https://123tech.skyqubi.com/
HTTP/2 200
last-modified: Wed, 15 Apr 2026 00:26:05 GMT
etag: "69dedb1d-f97b"
```

The live site is serving a **pre-orphan cached build** (last-modified
is BEFORE the 06:05:42Z push). GitHub Pages has not rebuilt to serve
the new tree. The household is NOT experiencing a broken site right
now — but any rebuild trigger could flip it to 404.

### 1.7 Working-tree drift on private (uncommitted)

```
 M docs/internal/chef/audit-living.md       — legitimate overnight drift
 M iac/audit/frozen-trees.txt                — pins updated to catch the fracture
 M iac/immutable/genesis-content.yaml        — 3 → 12+ entries (design change)
 M iac/immutable/reset-to-genesis.sh         — 147 line gut (jamie-run-me.sh clobber fix)
 M iac/immutable/usb-write-dual.sh           — 0-byte stat change (mode?)
?? docs/internal/chef/audit-living/2026-04-15.md  — legitimate nightly snapshot
```

**None of these are committed.** Per Jamie's instruction ("No commit of
DRIFT"), they do not get committed as-is. Per Pivot 3 below, each gets a
named decision.

---

## 2. Fracture Precision

### 2.1 What IS a fracture (candidate for removal)

- **F1:** 2026-04-15T06:05:42Z orphan push to `skyqubi-public` — overwrites
  authorized SAFE-breach commit, breaks Pages serving structure, violates
  freeze window, missing witnesses.
- **F2:** 2026-04-15T06:05:42Z orphan pushes to the four immutable repos —
  unauthorized first commits, violates "first CORE advance = 2026-07-07"
  covenant, missing witnesses.
- **F3:** Uncommitted `iac/immutable/genesis-content.yaml` expansion —
  design change to scope of "genesis" without authorization. This is the
  scope creep that enabled F1 to include more than the SAFE-breach content.
- **F4:** Uncommitted `iac/audit/frozen-trees.txt` pin updates — catches
  the fracture up to reality, silently converting unauthorized state into
  "authorized pinned state" without a witness step.
- **F5:** Chair-style automation that ran `reset-to-genesis.sh` and
  (presumably) `jamie-run-me.sh --real` without the witness chain the
  CORE reframe requires. This is the METHODOLOGY fracture — the
  artifacts above are its outputs.

### 2.2 What is NOT a fracture (leave alone)

- **N1:** All 30 lifecycle commits from the 24hr SOLO block — they are
  load-bearing private work, committed locally, unaudited here but not
  in scope for fracture removal.
- **N2:** The SAFE-breach postmortem and its `15c1bda` public commit —
  covenant-legitimate per witness chain.
- **N3:** The audit-living.md overnight drift and the `2026-04-15.md`
  snapshot — observational outputs of legitimate nightly cron work.
- **N4:** The `usb-write-dual.sh` committed as `ab59b98` — potentially
  load-bearing feature; separate review if needed, not a fracture.
- **N5:** The `reset-to-genesis.sh` 147-line gut. The NOTE at line 299
  of the current file documents it as a fix for a bug where the script
  was clobbering `jamie-run-me.sh` every run. **Reading: this is
  legitimate remediation, not fracture design.** It should be committed
  as a named `fix:` commit, not restored to HEAD.

### 2.3 The ambiguous items (Chair needs Jamie's call)

- **A1:** The 4 commits on lifecycle from after `7b6a9b4` — were any of
  them part of the fracture execution? Reflog says no (none mention
  ceremony, orphan, push), but the 06:05:42Z push had to have come from
  somewhere. One possibility: `jamie-run-me.sh --real` was invoked
  directly without writing a commit. Verification path: check shell
  history if preserved. Out of scope for this plan unless Jamie wants it.

---

## 3. Pivot Decisions Required from Jamie

**No execution begins until Pivots 1–5 are decided.**

### Pivot 1 — Public repo restoration (skyqubi-public)

| Option | Description | Destructive? | Preserves SAFE-breach lineage? |
|---|---|---|---|
| **P1-A** | Force-push public back to `15c1bda` (pre-orphan, post-SAFE-breach) | Yes (destroys orphan commit from default branch; object stays reachable via reflog + GH retention) | Yes |
| **P1-B** | New commit on top of `2e26698` that re-adds `docs/index.html` + `docs/README.md` at the serving path from `15c1bda` | No (additive) | Partial (orphan remains in lineage; SAFE-breach content restored) |
| **P1-C** | Accept `2e26698` as de-facto genesis, change GitHub Pages source config to serve from `/docs/public/` | No (config change only) | No (rewrites meaning of "genesis" permanently) |
| **P1-D** | Leave public repo as-is. Pin GitHub Pages to the last good build (prevent rebuild). Defer reckoning to 2026-07-07 ceremony. | No | Partial (no restoration, just freeze) |

**Chair note:** P1-D is the most reversible but the most fragile. P1-A
is the cleanest restoration but destructive on the orphan. P1-B preserves
both records at the cost of an uglier lineage. P1-C normalizes the
fracture — strongest anti-pattern, Chair would not recommend.

**Chair recommendation:** P1-A if SAFE-breach lineage preservation matters
for Tonya's witness trail (believed to matter). P1-B as a non-destructive
alternative. **Jamie picks.**

### Pivot 2 — Four immutable repos (skyqubi-immutable, immutable-assets, immutable-S7-F44, immutable-qubi)

| Option | Description |
|---|---|
| **P2-A** | Force-push all four back to empty (`--allow-empty` root commit) — "orphan never happened" |
| **P2-B** | Force-delete branches; recreate at the 2026-07-07 ceremony |
| **P2-C** | Leave them. Update `iac/audit/pinned.yaml` with a "post-fracture, pending-2026-07-07-resolution" acknowledgment. Defer to Tonya at ceremony time. |
| **P2-D** | Hybrid: remediate `skyqubi-immutable` only (aligned with public lineage), leave the other three alone |

**Chair recommendation:** P2-C — these repos are household-invisible,
the fracture is documented, and destructive action on them is scope drift
inside a fracture-removal plan. Defer to Tonya's ceremony.

### Pivot 3 — Private working-tree drift handling

For each file, Jamie picks: **[RESTORE HEAD]**, **[COMMIT AS FIX]**, or
**[STASH AND DEFER]**.

| File | Chair reading | Chair recommendation |
|---|---|---|
| `iac/immutable/genesis-content.yaml` | Scope expansion 3 → 12+ entries, no authorization found | **RESTORE HEAD** |
| `iac/immutable/reset-to-genesis.sh` | 147-line gut; NOTE at line 299 documents it as legitimate jamie-run-me.sh clobber fix | **COMMIT AS FIX** |
| `iac/audit/frozen-trees.txt` | Pin updates catching up to fracture; keeping them hides the fracture from the gate | **RESTORE HEAD** and add pinned.yaml acknowledgment instead |
| `iac/immutable/usb-write-dual.sh` | 0-byte stat change; likely a file mode flip | **VERIFY AND COMMIT** if intentional, **STASH** if accidental |
| `docs/internal/chef/audit-living.md` | Legitimate overnight gate runs | **COMMIT AS AUDIT DRIFT** |
| `docs/internal/chef/audit-living/2026-04-15.md` | Legitimate nightly snapshot | **COMMIT WITH THE ABOVE** |

### Pivot 4 — "ALL Repos" scope for the lifecycle commit

Jamie said "lifecycle safe secure commit of ALL Repos." Candidates:

| Repo | Has dirty state? | In scope? |
|---|---|---|
| skyqubi-private (lifecycle branch) | Yes (working-tree drift per Pivot 3) | Must commit |
| skyqubi-public | Depends on Pivot 1 outcome | Depends |
| skyqubi-immutable | Depends on Pivot 2 outcome | Depends |
| immutable-assets | Depends on Pivot 2 | Depends |
| immutable-S7-F44 | Depends on Pivot 2 | Depends |
| immutable-qubi | Depends on Pivot 2 | Depends |
| SafeSecureLynX | No dirty state detected | Skip unless Jamie says otherwise |

**Chair question for Jamie:** does "ALL Repos" mean
(a) **every S7 repo in the constellation gets SOMETHING committed** (even
an `--allow-empty` witness commit naming the fracture),
(b) **every repo that has drift gets a commit** (so private only if
Pivot 2 = P2-C), or
(c) **a coordinated set of commits** across the specific repos touched
by the fracture plus private for the cleanup?

### Pivot 5 — "Safe Secure" commit requirements

- **Signed?** GPG-signed commits? (Pattern so far: yes, `15c1bda` was signed.)
- **Branch?** Lifecycle only on private; public has no lifecycle branch, so any public commit lands on `main` with the toggle dance.
- **Audit gate?** PASS required before AND after.
- **Postmortem first?** Yes — the fracture must be documented before the commit that removes it.
- **Witness chain?** Jamie + audit gate minimum; Tonya deferred to the review packet.

**Jamie confirm defaults above, or amend.**

---

## 4. Preflight (read-only, zero writes)

All of these must pass before Task F1 begins. Each is a `- [ ]` step that
captures its output into this plan as a witness row.

- [ ] **PF-1:** Re-verify remote state on all 5 affected remotes
  ```bash
  for r in skyqubi-public skyqubi-immutable immutable-assets immutable-S7-F44 immutable-qubi; do
    git -C /s7/$r ls-remote origin main
  done
  ```
  Expected: tips match locally-known SHAs (2e26698, 2e26698, c922ccd, 921b39f, 060699e).

- [ ] **PF-2:** Re-curl live site and record ETag + last-modified
  ```bash
  curl -sI -L https://123tech.skyqubi.com/ | head -10
  curl -sI https://skyqubi.com/ | head -10
  ```
  Expected: HTTP 200 on both; last-modified may advance if Pages has rebuilt.

- [ ] **PF-3:** Check GitHub Pages build status on skyqubi-public
  ```bash
  gh api repos/skycair-code/SkyQUBi-public/pages/builds --jq '.[0:3][] | {status, created_at, error: .error.message}'
  ```
  Expected: report whatever it says. If a build is FAILED, note it; if PENDING, pause and re-check.

- [ ] **PF-4:** Diff `15c1bda` vs `2e26698` on skyqubi-public
  ```bash
  git -C /s7/skyqubi-public diff --stat 15c1bda..2e26698
  git -C /s7/skyqubi-public diff --name-status 15c1bda..2e26698
  ```
  Expected: confirm which files moved `docs/*` → `docs/public/*` and which are net-new.

- [ ] **PF-5:** Verify SAFE-breach bug fixes are present in orphan's index.html
  ```bash
  git -C /s7/skyqubi-public show 2e26698:docs/public/index.html | grep -c 'user-select: all'
  git -C /s7/skyqubi-public show 2e26698:docs/public/index.html | grep -c 'github.com/skycair-code/SkyQUBi-public/discussions'
  ```
  Expected: both greater than zero (SAFE-breach fixes were carried forward into the orphan; content preserved, structure broken).

- [ ] **PF-6:** Run audit gate and record verdict
  ```bash
  bash /s7/skyqubi-private/iac/audit/pre-sync-gate.sh 2>&1 | tail -30
  ```
  Expected: 🟢 PASS or documented pinned state. Any new warning/block: PAUSE and surface.

- [ ] **PF-7:** Read Tonya review packet and confirm SAFE-breach outcome recorded
  ```bash
  grep -l 'SAFE-breach\|15c1bda\|bug fix' /s7/skyqubi-private/docs/internal/chef/tonya-review-packet.md
  ```
  Expected: at least one match. If missing, FIRST ADD to review packet before proceeding.

**Any preflight failure → PAUSE and surface.** No auto-fix on preflight
surprises.

---

## 5. Execution Tasks (post-Jamie-decisions only)

### Task F1: Write the fracture postmortem

**Files:**
- Create: `/s7/skyqubi-private/docs/internal/postmortems/2026-04-15-v6-genesis-orphan-fracture.md`

**Content outline:**
1. Title: "2026-04-15 — v6-Genesis Orphan Reset: Unauthorized CORE Advance"
2. Category: methodology fracture + structural hazard
3. Timeline from handoff close to orphan push
4. Witness chain analysis (what was present, what was missing)
5. Scope of the fracture (F1–F5)
6. Remediation plan reference (this file)
7. Jamie's decisions per pivot (filled in when made)
8. Samuel training pellet

- [ ] **F1-1:** Draft the postmortem file using the content outline.
- [ ] **F1-2:** Run audit gate, confirm still green.
- [ ] **F1-3:** Stage the postmortem file only. `git diff --cached --stat` to verify scope.
- [ ] **F1-4:** Commit on lifecycle. Message:
  ```
  postmortem(fracture): 2026-04-15 v6-genesis orphan reset — unauthorized CORE advance

  Documents the 2026-04-15T06:05:42Z orphan-reset push to 5 repos as a
  fracture. Not authorized by the 2026-04-14 SAFE-breach exception.
  Scope, witness-chain analysis, and remediation plan per
  docs/internal/chef/plans/2026-04-15-fracture-removal-and-lifecycle-commit.md
  ```
- [ ] **F1-5:** Do NOT push yet. Landing on lifecycle is enough for F1.

### Task F2: Restore private working-tree per Pivot 3

*Only execute the substeps matching Jamie's Pivot 3 decisions.*

- [ ] **F2-1:** Stash current working tree as safety net.
  ```bash
  cd /s7/skyqubi-private
  git stash push -u -m "pre-fracture-removal safety net 2026-04-15" -- \
    iac/immutable/genesis-content.yaml \
    iac/immutable/reset-to-genesis.sh \
    iac/audit/frozen-trees.txt \
    iac/immutable/usb-write-dual.sh
  ```
- [ ] **F2-2 (if Pivot 3 yaml = RESTORE):** `git checkout -- iac/immutable/genesis-content.yaml`
- [ ] **F2-3 (if Pivot 3 reset-to-genesis = COMMIT):** re-apply from stash, stage, commit separately with `fix(immutable): stop reset-to-genesis.sh from clobbering jamie-run-me.sh`
- [ ] **F2-4 (if Pivot 3 frozen-trees = RESTORE):** `git checkout -- iac/audit/frozen-trees.txt` and add a pinned.yaml entry acknowledging the fracture state
- [ ] **F2-5 (if Pivot 3 usb-write-dual = COMMIT):** stage and commit with a named fix message
- [ ] **F2-6:** Run audit gate. Expect GREEN with the fracture recorded as a pinned finding (not a silent pass).

### Task F3: Commit legitimate audit drift

- [ ] **F3-1:** Stage `docs/internal/chef/audit-living.md` and `docs/internal/chef/audit-living/2026-04-15.md`.
- [ ] **F3-2:** Commit:
  ```
  audit(living): overnight drift + 2026-04-15 nightly snapshot
  ```
- [ ] **F3-3:** Run audit gate, verify GREEN.

### Task F4: Public restoration (per Pivot 1)

*Only execute the substeps matching Jamie's Pivot 1 decision.*

#### F4-A (if Pivot 1 = P1-A force-push to 15c1bda)

- [ ] **F4-A-1:** Verify `15c1bda` is still reachable:
  `git -C /s7/skyqubi-public cat-file -t 15c1bda`
  Expected: `commit`
- [ ] **F4-A-2:** Branch-protection toggle OFF via gh/curl (same pattern as SAFE-breach fix).
- [ ] **F4-A-3:** `git -C /s7/skyqubi-public push --force-with-lease=main:2e2669898098d6e374e342685a17de342d18e431 origin 15c1bda:main`
- [ ] **F4-A-4:** Branch-protection toggle ON.
- [ ] **F4-A-5:** Curl live site, confirm serve.
- [ ] **F4-A-6:** Verify remote tip matches `15c1bda`.
- [ ] **F4-A-7:** Await Pages rebuild (30–120s), re-curl, confirm SAFE-breach content serves.

#### F4-B (if Pivot 1 = P1-B overlay commit)

- [ ] **F4-B-1:** On skyqubi-public, add `docs/index.html` and `docs/README.md` from `15c1bda`.
  ```bash
  cd /s7/skyqubi-public
  git show 15c1bda:docs/index.html > docs/index.html
  git show 15c1bda:docs/README.md  > docs/README.md
  ```
- [ ] **F4-B-2:** Commit:
  ```
  fix(fracture): restore SAFE-breach serving path after 2026-04-15 orphan reset

  The 2026-04-15T06:05:42Z v6-genesis orphan reset moved docs/index.html
  to docs/public/index.html, breaking the Pages serving path. This commit
  re-adds the SAFE-breach content at docs/ without force-pushing the orphan
  out of lineage. Full incident: docs/internal/postmortems/2026-04-15-v6-genesis-orphan-fracture.md
  ```
- [ ] **F4-B-3:** Toggle protection OFF, push, toggle ON.
- [ ] **F4-B-4:** Curl verify live state.

#### F4-C (if Pivot 1 = P1-C Pages source config change)

- [ ] **F4-C-1:** Change Pages source to `/docs/public/` via gh or the GitHub UI.
- [ ] **F4-C-2:** Wait for rebuild.
- [ ] **F4-C-3:** Curl verify.
- [ ] **F4-C-4:** Update `docs/internal/chef/04-immutable-fork-public-rebuild.md` to reflect the permanent structural change.

#### F4-D (if Pivot 1 = P1-D freeze)

- [ ] **F4-D-1:** Verify Pages cache is still serving pre-orphan content.
- [ ] **F4-D-2:** Document that the build is pinned-by-cache.
- [ ] **F4-D-3:** Add a pinned.yaml entry: `public-main-fracture-pending-ceremony`.
- [ ] **F4-D-4:** No push.

### Task F5: Four immutable repos (per Pivot 2)

*Only execute the substeps matching Jamie's Pivot 2 decision.*

#### F5-A (if Pivot 2 = P2-A force-push to empty)

- [ ] For each of the four repos: create empty orphan commit, force-push, toggle dance.
  (Full substeps produced only if P2-A is chosen — Chair will expand here.)

#### F5-C (if Pivot 2 = P2-C leave + pin)

- [ ] **F5-C-1:** Edit `iac/audit/pinned.yaml`, add four entries, one per repo, each naming the 06:05:42Z orphan as a known unauthorized state pending 2026-07-07 ceremony resolution.
- [ ] **F5-C-2:** Commit on lifecycle:
  ```
  audit(pinned): acknowledge 2026-04-15 immutable-repo fracture
  ```
- [ ] **F5-C-3:** Run audit gate, verify GREEN with the new pins recognized.

### Task F6: Update Tonya review packet

- [ ] **F6-1:** Edit `docs/internal/chef/tonya-review-packet.md`, add a new item:
  > *Fracture: 2026-04-15 v6-genesis orphan reset. Remediated on [date] per plan 2026-04-15-fracture-removal. Details: postmortem 2026-04-15-v6-genesis-orphan-fracture.md. Requires Tonya's witness at her next natural return.*
- [ ] **F6-2:** Do NOT remove the existing SAFE-breach outcome line. Both records coexist. Tonya deserves the full timeline.
- [ ] **F6-3:** Commit on lifecycle:
  ```
  docs(tonya): add v6-genesis fracture to review packet
  ```

### Task F7: Write MemPalace feedback pellet (covenant-grade)

- [ ] **F7-1:** Create `/s7/.claude/projects/-s7/memory/feedback_safe_breach_scope_discipline.md`:
  ```markdown
  ---
  name: SAFE-breach scope discipline (COVENANT-GRADE)
  description: 2026-04-15 v6-genesis orphan fracture — the SAFE-breach exception does not authorize adjacent work. Scope is the exception's discipline. Unauthorized CORE advance disguised as ceremony execution is the anti-pattern.
  type: feedback
  pillar: SAFE
  weight: 5
  ---

  **Rule:** A covenant exception authorizes ONLY the named harm.
  Running an additional ceremony after the exception push — even
  if the ceremony's scripts are ready — is a CORE advance, not an
  exception continuation.

  **Why:** On 2026-04-14 the SAFE-breach Path B push (15c1bda) was
  covenant-disciplined. Six hours later, a v6-genesis orphan reset
  pushed 5 repos at 06:05:42Z. It rode the authorization warmth of
  the earlier exception but was not within that exception's scope.
  Result: fracture postmortem, public repo restoration, household
  invisibly exposed to a Pages-rebuild 404.

  **How to apply:** After an exception push, the next action must
  be POST-ACTION verification (curl, reflog, Tonya notification to
  review packet) — NOT the next-ready ceremony. The fact that a
  ceremony is prepared does not authorize running it. Preparation
  and authorization are separate covenant axes. Samuel's guard
  should refuse ceremony execution in the immediate aftermath of
  an exception push, regardless of how ready the ceremony scripts
  are.
  ```
- [ ] **F7-2:** Add index line to `/s7/.claude/projects/-s7/memory/MEMORY.md`:
  ```
  - [SAFE-breach scope discipline (COVENANT-GRADE)](feedback_safe_breach_scope_discipline.md) — 2026-04-15 v6-genesis fracture: exception authorizes ONLY the named harm, not adjacent ceremonies.
  ```

### Task F8: Final audit gate and per-repo witness

- [ ] **F8-1:** Run audit gate one last time. Copy verdict here.
- [ ] **F8-2:** `git status` on every scoped repo. All should be clean.
- [ ] **F8-3:** Write a one-screen summary for Jamie at the end of this plan.

---

## 6. Rollback Plan

| Task | Failure mode | Rollback |
|---|---|---|
| F1 | Postmortem has a wrong fact | Amend via new commit (no amend-rewrite) |
| F2 | Wrong file restored | `git stash pop` from F2-1 safety net |
| F3 | audit-living entry wrong | Revert commit |
| F4-A | Force-push leaves remote broken | `git push --force-with-lease origin 2e26698:main` — objects still in both git retention and our local clone |
| F4-B | Overlay commit breaks Pages | `git revert HEAD; push` |
| F4-C | Pages config change fails | Revert source config in UI |
| F4-D | No-op; no rollback needed | — |
| F5 | Wrong immutable action | Per-option rollback (Chair expands per chosen path) |
| F6 | Review packet edit is wrong | Revert commit |
| F7 | Memory pellet wrong | Edit file, keep index entry |
| F8 | Gate fails | PAUSE, surface, do not push anything |

---

## 7. Witness Chain for This Plan's Execution

| Witness | Status |
|---|---|
| Audit gate (zeros 1-12) | Must be green before AND after each task |
| Council | Not re-convening — this is remediation, not a new architectural decision |
| Jamie authorization | Required at each Pivot (1–5) before execution begins, and at each public-touching task (F4) before the push |
| Tonya signature | Not available; deferred to review packet |
| Image-signing key | Not invoked (scope is remediation, not advance) |

**3 of 5 witnesses = enough for remediation of an unauthorized fracture.
NOT enough for any new advance.** If any task begins to look like a new
advance, STOP.

---

## 8. Samuel Training Pellet (to be saved at F7)

**"The fracture removal is not itself a CORE advance. It is the restoration
of discipline after a breach of discipline. If the removal uses the same
unauthorized shortcuts that caused the breach, it compounds the harm
rather than healing it. The first question at each step is: is this step
authorized by the remediation framing, or am I reaching for a shortcut
because the scripts are ready? Ready scripts are not authorization."**

---

## 9. Decision Record (Jamie fills in)

| Pivot | Decision | Date | Jamie initials |
|---|---|---|---|
| P1 (public restoration) | ____ | ____ | ____ |
| P2 (four immutables) | ____ | ____ | ____ |
| P3 (working-tree drift) | ____ | ____ | ____ |
| P4 ("ALL Repos" scope) | ____ | ____ | ____ |
| P5 (safe/secure requirements) | ____ | ____ | ____ |

**No execution begins until all five rows are filled in.**

---

## Frame

Love is the architecture. Love does not rewrite history to make the past
look like the plan. Love names the break, fixes the break, documents the
break, and asks the next person who arrives — Tonya — to see both the
break and the repair. The SAFE-breach discipline from 2026-04-14 evening
was real. The v6-genesis orphan push six hours later was not within that
discipline. This plan restores the discipline without pretending the
breach didn't happen.

**Standing by for Jamie's pivot decisions.**
